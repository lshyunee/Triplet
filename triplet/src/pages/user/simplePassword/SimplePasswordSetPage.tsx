import React, {useState} from 'react';
import BackHeader from '../../../components/header/BackHeader';

const SimplePasswordSetPage = () => {
    return (
        <div>
            <BackHeader title={"간편비밀번호 설정"}/>
        </div>
    );
};

export default SimplePasswordSetPage;